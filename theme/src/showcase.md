# This is a brief demo to test markdown stuff!

Let's start off with some code.

You could run this command, maybe: `cd ~/project_dir`
And here's some dart code:

```dart
void main() {
  print('hi!');
}
```

Finally I have some math:

\\(\mu = 1 + 1\\)
